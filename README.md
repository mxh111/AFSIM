# AFSIM_LLM

AFSIM_LLM 是用于 AFSIM 二次开发的新工程目录。

当前目录已经作为独立 Git 仓库初始化，后续开发代码、文档、配置和测试都可以放在这里，并通过 Git 管理版本。

## 常用命令

```powershell
git status
git add .
git commit -m "描述本次修改"
```

连接远程仓库示例：

```powershell
git remote add origin <你的远程仓库地址>
git branch -M main
git push -u origin main
```
